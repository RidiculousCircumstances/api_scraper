{# templates/admin/my-custom-page.html.twig #}
{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}Конфигурация{% endblock %}

{% block main %}

    {% if message != null %}
        {% if message.status.value == 0 %}
            <div class="alert alert-success">
                <span>{{ message.message }}</span>
            </div>
        {% elseif message.status.value == 1 %}
            <div class="alert alert-warning" role="alert">
                <span> Последняя задача завершилась ошибкой [{{ message.dateTime }}]: {{ message.message }}</span>
            </div>
        {% elseif message.status.value == 2 %}
            <div class="alert alert-info">
                <span>Обработка...</span>
            </div>
        {% endif %}
    {% endif %}

    {{ form_start(form) }}
    <div class="row">
    <div class="col-md-7 col-xxl-6">
        <br>
        {{ form_errors(form) }}

        <div class="form-group">
            {{ form_row(form.schema, { 'attr': {'class': 'form-control'} } ) }}
        </div>

        <div class="form-group">
            {{ form_row(form.method, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.secret, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.auth, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.format, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.file, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.delay, { 'attr': {'class': 'form-control'} }) }}
        </div>

        <div class="form-group">
            {{ form_row(form.useProxy, { 'attr': {'class': 'form-checkbox'} }) }}
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}